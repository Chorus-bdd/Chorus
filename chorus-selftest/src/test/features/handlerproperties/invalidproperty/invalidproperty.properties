#for testing processes with configurations
processes.config1.logging=false
processes.config1.myinvalidprop=wibble

remoting.config1.wibble=wibble

#test the default property group by setting mainclass as a default property
#this should be picked up for config1 group so that the config1 configuration is
#then valid (otherwise it would be invalid having no mainclass)
processes.default.mainclass=handlerproperties.invalidproperty.StartAProcessMain
